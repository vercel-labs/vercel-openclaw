import test from "node:test";

import "../../scripts/check-channel-delivery-docs";

test("channel delivery docs match generated state machine artifacts", () => {
  // Importing the script performs the drift assertions.
});
